#pragma once
#include <vector>
#include <SFML/Graphics.hpp>
const std::vector<sf::Color> Color1 = {sf::Color::Black,
                                       sf::Color::Red,
                                       sf::Color::Green,
                                       sf::Color::Blue,
                                       sf::Color::Yellow,
                                       sf::Color::Cyan,
                                       sf::Color::White,
                                       sf::Color::Magenta};

enum class Color
{
    BLACK,
    RED,
    GREEN,
    BLUE,
    YELLOW,
    CYAN,
    WHITE,
    MAGENTA
};

